/*
        auth.js — Login / Logout (MVP)
     -Controla:
    validação do login (por enquanto simples/demo)
    esconder/mostrar tela de login
    guardar info do usuário em memória/localStorage (se você quiser)
    ação de logout
    MVP: login simples só pra restringir acesso básico ao painel. 
*/